const prevMapButton = document.querySelector('#prev_map');
const nextMapButton = document.querySelector('#next_map');
const mapRangeButton = document.querySelector('#switch_btn_range_map');
const mapPerButton = document.querySelector('#switch_btn_per_map');
const nextListButton = document.querySelector('.button2');
const prevListButton = document.querySelector('.button1');
const listRangeButton = document.querySelector('#switch_btn_range');
const listPerButton = document.querySelector('#switch_btn_per');
const searchButton = document.querySelector('.search');
const globalRange = document.querySelector('#switch_btn_range_global');
const mapRange = document.querySelector('#switch_btn_range_map');
const perGlobal = document.querySelector('#switch_btn_per_global');
const perMap = document.querySelector('#switch_btn_per_map');
const reset = document.querySelector('.reset');
const previousGraph = document.querySelector('.previous-graph');
const nextGraph = document.querySelector('.next-graph');

export {
  prevMapButton,
  nextMapButton,
  mapRangeButton,
  mapPerButton,
  nextListButton,
  prevListButton,
  listRangeButton,
  listPerButton,
  searchButton,
  globalRange,
  mapRange,
  perGlobal,
  perMap,
  reset,
  previousGraph,
  nextGraph,
};
